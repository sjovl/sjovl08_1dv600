// let template = require('./html.js')

class Hangman {
  constructor () {
    this.word = []
    this.letters = []
    // this.template = template.firstElementChild.cloneNode(true)
  }
  init () {
    console.log('hej')
    // document.querySelector('game').appendChild(this.template)
  }
}

module.exports = Hangman
